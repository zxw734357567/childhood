spring:
  application:
    name: jojo-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/zxw734357567/jojo-config-server.git
          search-paths: Repository
      label: master